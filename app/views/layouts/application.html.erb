<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="utf-8">
    <title><%=t :app_name%></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://ga.jspm.io/npm:flatpickr@4.6.9/dist/flatpickr.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body id="<%=controller.controller_name%>_<%=controller.action_name%>">
    <div id='current_page'
         data-controller="<%= controller.controller_name %>"
         data-action="<%= controller.action_name %>">
    </div>
    <div id='current_user'
         data-user_id="<%= current_user.id if current_user %>"
         data-role_id="<%= current_user.role_id if current_user %>">
    </div>
    <% if current_user %>
      <%= render 'layouts/navbar' %>
      <%= render 'layouts/content' %>
      <%= render 'layouts/footer' %>
    <% else %>
      <%= render 'layouts/login' %>
    <% end %>
  </body>
</html>
